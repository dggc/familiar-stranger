% collect data
[timeMatrix, numbMatrix] = generateTimeMatrix( s, network);

% create a map to convert mac to subject
my_map = MacMap (s, network);

% create a matrix with time data

% create a matrix with frequency data

% sort data